import SalaryEmailPortal from '@/components/views/SalaryEmailPortal';

export const metadata = { title: 'Gửi Tin Nội Bộ — Cổng nhân viên' };
export const dynamic = 'force-dynamic';

export default function SalaryPage() {
  return <SalaryEmailPortal />;
}
