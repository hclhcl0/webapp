import ZaloChatPortal from '@/components/views/ZaloChatPortal';

export const metadata = { title: 'Chat CSKH — Cổng nhân viên' };
export const dynamic = 'force-dynamic';

export default function ChatPortalPage() {
  return <ZaloChatPortal />;
}
